{
  "name": "workhorse-station",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.6.5",
  "scripts": {
    "dev": "pnpm --parallel --filter @workhorse-station/api --filter @workhorse-station/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "desktop:build": "pnpm --filter @workhorse-station/desktop build",
    "desktop:start": "pnpm --filter @workhorse-station/desktop start"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
